'use strict';

/**
 * These are the keys for Rockford's mappable data
 */

const maps = {
    wards: 'whmi-gmnb',
    rpdDistricts: 'fwiz-44cq'
};

exports.mapKeys = Object.keys(maps);
exports.availableMaps = maps;
